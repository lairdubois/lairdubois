{% extends 'Common/Modal/_modal.part.html.twig' %}

{% set id = 'referral_modal' %}
{% set title = 'Redirection' %}

{% block bodyInner %}
    Vous venez d'être redirigé sur <em>L'Air&nbsp;du&nbsp;Bois</em> par le site <strong>{{ referral.referer.title|raw }}</strong>.
    <br>
    <br>
    Souhaitez vous continuer ?
{% endblock %}

{% block footerInner %}
    <a href="{{ referral.url }}" class="btn btn-default" data-loading-text="{{ 'default.loading'|trans() }}" onclick="$(this).button('loading');">Non, retourner sur {{ referral.referer.title|u.truncate(20) }}</a>
    <a href="#" class="btn btn-primary" data-dismiss="modal">{{ 'default.continue'|trans() }}</a>
{% endblock %}