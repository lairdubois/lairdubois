{% extends 'Core/Review/_form.html.twig' %}

{% set i18nPrefix = i18nPrefix|default(ladb_entity_type_stripped_name(ladb_entity(review.entityType, review.entityId))) %}
{% set formAction = path('core_review_update', { 'id':review.id }) %}

{% block title %}<i class="ladb-icon-edit"></i> {{ ('review.'~i18nPrefix~'.edit')|trans() }}{% endblock %}

{% block buttons %}
    {{ parent() }}
    {% include 'Common/_form-btn-loading.part.html.twig' with { 'text':('default.save'|trans()), 'classes':'btn-primary' } %}
{% endblock %}
