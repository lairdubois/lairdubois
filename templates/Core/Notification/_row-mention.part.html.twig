{% extends 'Core/Notification:_row.part.html.twig' %}

{% set icon = 'mention' %}
{% set entity = ladb_entity(notification.activity.mention.entityType, notification.activity.mention.entityId) %}

{% block body %}
    a mentionné {% if notification.activity.mention.mentionedUser == app.user %}votre nom{% else %}<em>{{ notification.activity.mention.mentionedUser.displayName }}</em>{% endif%} dans
    {% set isYour = entity.user is defined and entity.user == app.user %}
    {{ ('notification.'~(isYour ? 'your' : 'the')~'.'~ladb_entity_type_stripped_name(entity))|trans() }} "<strong>{{ entity.title }}</strong>".
{% endblock %}