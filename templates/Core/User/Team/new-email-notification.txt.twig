{% extends ':email-layout.txt.twig' %}

{% block content %}
    Un nouveau collectif vient d'être créé par {{ actorUser.displayname|raw }} : {{ team.displayname|raw }}.

    {{ url('core_user_show', { 'username':team.usernameCanonical }) }}

    CREATOR : {{ url('core_user_show', { 'username':actorUser.usernameCanonical }) }}
    REMOTE_ADDR : {{ app.request.server.get('REMOTE_ADDR') }}
    HTTP_USER_AGENT : {{ app.request.server.get('HTTP_USER_AGENT') }}
{% endblock %}
