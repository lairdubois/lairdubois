{% apply spaceless %}

{# set entity #}

<span class="ladb-published-age{% if classes is defined %} {{ classes }}{% endif %}">Publié <time datetime="{{ entity.createdAt|date('Y-m-d H:i:s') }}">{{ entity.createdAt|ago() }}</time></span>
{% endapply %}
