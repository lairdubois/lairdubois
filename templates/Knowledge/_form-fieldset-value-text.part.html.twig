{% extends 'Knowledge/_form-fieldset-value.part.html.twig' %}

{% set fieldDef = knowledge is defined ? knowledge.fieldDefs[field] : fieldDef|default(null) %}

{% block dataFormGroup %}
    {{ form_errors(form.data) }}
    {% if fieldDef is not null and fieldDef[constant('\\App\\Entity\\Knowledge\\AbstractKnowledge::ATTRIB_CHOICES')] is defined %}
        {{ form_widget(form.data) }}
    {% else %}
        {{ form_widget(form.data, { 'attr':{ 'class':'form-control input-lg', 'placeholder':fieldLabel } }) }}
    {% endif %}
{% endblock %}
