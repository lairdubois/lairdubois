IMPORTANT:
----------

Le contenu original de cette archive a été publié par *{{ plan.user.displayName }}*{% if plan.user.fullName %} ({{ plan.user.fullName }}){% endif %} dans le plan *{{ plan.title }}* ({{ ladb_entity_url_action(plan) }}).

Cette archive contient {{ plan.resources|length }} {{ 'default.choice.files'|transchoice(plan.resources|length) }} :
{% for resource in plan.resources %}
 - {{ resource.fileName }}
{% endfor %}

L'Air du Bois - Découvrez, fabriquez, partagez.
http://www.lairdubois.fr