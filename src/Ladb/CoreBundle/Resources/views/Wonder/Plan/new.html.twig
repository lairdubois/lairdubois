{% extends 'LadbCoreBundle:Wonder/Plan:_form.html.twig' %}

{% set pageTitle = 'plan.new'|trans() %}
{% set formAction = path('core_plan_create') %}
{% set hideWarning = hideWarning|default(false) %}

{% block sidenavHeading %}
    <i class="ladb-icon-plus"></i> {{ 'plan.new'|trans() }}
{% endblock %}

{% block form_buttons %}
    {% include 'LadbCoreBundle:Common:_form-btn-loading.part.html.twig' with { 'text':('default.save'|trans()), 'classes':'ladb-form-save-btn btn-primary btn-block' } %}
{% endblock %}

{% block body %}
    {{ parent() }}
    {% if not hideWarning %}
        {% include 'LadbCoreBundle:Common:_new-warning-modal.part.html.twig' with { 'id':'warning_plan_modal', 'title':('plan.new'|trans()), 'textTransKey':'plan.new_warning', 'cancelPath':'core_plan_list' } %}
    {% endif %}
{% endblock %}

{% block bodyJavascripts %}
    {{ parent() }}
    {% if not hideWarning %}
        <script>
            $(document).ready(function() {
                $("#warning_plan_modal").modal();
            });
        </script>
    {% endif %}
{% endblock %}
