{% extends 'LadbCoreBundle:Common:_list-n.part.html.twig' %}

{# set entity #}
{# set collections #}
{# set nextPageUrl #}

{% block items %}
    {% for collection in collections %}
        <div class="ladb-collection-row">
            <div class="media">
                <div class="media-left">
                    {% include 'LadbCoreBundle:Collection/Entry:_widget.part.html.twig' with { 'entryContext':entryContexts[loop.index0] } %}
                </div>
                <div class="media-body">
                    <div style="font-size: 110%;">{{ collection.title }}</div>
                    <div style="font-size: 80%;">{% include 'LadbCoreBundle:Collection/Collection:_publications-counter.part.html.twig' %}</div>
                </div>
                <div class="media-right">
                    {% include 'LadbCoreBundle:Collection:Collection/_visibility-badge.part.html.twig' %}
                </div>
            </div>
        </div>
    {% endfor %}
{% endblock %}
