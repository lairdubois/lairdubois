{% extends 'LadbCoreBundle:Core/Comment:_new.part.html.twig' %}

{# set comment #}
{# set mentionStrategy #}

{% set condenced = false %}

{% block script %}
    <script>
        setupTextareas();
        $("#{{ form.body.vars.id }}").markdown().ladbTextcompletify({ mentionStrategy: {{ mentionStrategy|raw }} }).focus();
    </script>
{% endblock %}
