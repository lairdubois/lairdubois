{% extends 'LadbCoreBundle:Core/Notification:_email.html.twig' %}

{% block rowContent %}
    <p style="margin: 15px 0 5px 0;">
        <strong>{{ row.actorUser.displayname }}</strong> a publié {{ ('notification.one_new.'~ladb_entity_type_stripped_name(row.entity))|trans() }}.
    </p>
    <table cellspacing="10" cellpadding="0">
        <tbody>
        {% include 'LadbCoreBundle:Common:_entity-email-row.part.html.twig' with { 'entity':row.entity } %}
        </tbody>
    </table>
{% endblock %}