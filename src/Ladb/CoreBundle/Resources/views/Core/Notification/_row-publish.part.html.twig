{% extends 'LadbCoreBundle:Core/Notification:_row.part.html.twig' %}

{% set icon = 'plus' %}
{% set entity = ladb_entity(notification.activity.entityType, notification.activity.entityId) %}

{% block body %}
    a publié {{ ('notification.one_new.'~ladb_entity_type_stripped_name(entity))|trans() }}{% if entity.title is defined %} "<strong>{{ entity.title }}</strong>"{% endif %}.
{% endblock %}