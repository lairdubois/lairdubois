{% extends 'LadbCoreBundle:Workshop:_form.html.twig' %}

{% set pageTitle = 'workshop.edit'|trans() %}
{% set formAction = path('core_workshop_update', { 'id':workshop.id }) %}

{% block sidenavHeading %}
    <i class="ladb-icon-edit"></i> {{ 'workshop.edit'|trans() }}
{% endblock %}

{% block form_buttons %}
    <a href="{{ path('core_workshop_show', { 'id':workshop.sluggedId }) }}" class="btn btn-default btn-block">{{ 'default.back'|trans() }}</a>
    {% include 'LadbCoreBundle:Common:_form-btn-loading.part.html.twig' with { 'text':('default.update'|trans()), 'classes':'ladb-form-save-btn btn-primary btn-block' } %}
{% endblock %}

{% block sections %}
    {% if workshop.isDraft %}
        {% include 'LadbCoreBundle:Workshop:_draft-alert.part.html.twig' %}
    {% endif %}
    {{ parent() }}
{% endblock %}
