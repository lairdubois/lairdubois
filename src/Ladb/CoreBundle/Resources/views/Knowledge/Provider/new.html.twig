{% extends 'LadbCoreBundle:Knowledge/Provider:_form.html.twig' %}

{% set pageTitle = 'knowledge.provider.new'|trans() %}
{% set formAction = path('core_provider_create') %}
{% set hideWarning = hideWarning|default(false) %}

{% block sidenavHeading %}
    <i class="ladb-icon-plus"></i> {{ 'knowledge.provider.new'|trans() }}
{% endblock %}

{% block form_buttons %}
    {% include 'LadbCoreBundle:Common:_form-btn-loading.part.html.twig' with { 'text':('default.save'|trans()), 'classes':'ladb-form-save-btn btn-primary btn-block' } %}
{% endblock %}

{% block body %}
    {{ parent() }}
    {% if not hideWarning %}
        {% include 'LadbCoreBundle:Common:_new-warning-modal.part.html.twig' with { 'id':'warning_provider_modal', 'title':('knowledge.provider.new'|trans()), 'textTransKey':'knowledge.provider.new_warning', 'cancelPath':'core_provider_list' } %}
    {% endif %}
{% endblock %}

{% block bodyJavascripts %}
    {{ parent() }}
    {% if not hideWarning %}
        <script>
            $(document).ready(function() {
                $("#warning_provider_modal").modal();
            });
        </script>
    {% endif %}
{% endblock %}

