{% extends 'LadbCoreBundle:Knowledge/Tool:_form.html.twig' %}

{% set pageTitle = 'knowledge.tool.new'|trans() %}
{% set formAction = path('core_tool_create') %}
{% set hideWarning = hideWarning|default(false) %}

{% block sidenavHeading %}
    <i class="ladb-icon-plus"></i> {{ 'knowledge.tool.new'|trans() }}
{% endblock %}

{% block form_buttons %}
    {% include 'LadbCoreBundle:Common:_form-btn-loading.part.html.twig' with { 'text':('default.save'|trans()), 'classes':'ladb-form-save-btn btn-primary btn-block' } %}
{% endblock %}

{% block body %}
    {{ parent() }}
    {% if not hideWarning %}
        {% include 'LadbCoreBundle:Common:_new-warning-modal.part.html.twig' with { 'id':'warning_tool_modal', 'title':('knowledge.tool.new'|trans()), 'textTransKey':'knowledge.tool.new_warning', 'cancelPath':'core_tool_list' } %}
    {% endif %}
{% endblock %}

{% block bodyJavascripts %}
    {{ parent() }}
    {% if not hideWarning %}
        <script>
            $(document).ready(function() {
                $("#warning_tool_modal").modal();
            });
        </script>
    {% endif %}
{% endblock %}


