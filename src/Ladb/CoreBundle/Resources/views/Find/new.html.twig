{% extends 'LadbCoreBundle:Find:_form.html.twig' %}

{% set pageTitle = 'find.new'|trans() %}
{% set formAction = path('core_find_create') %}
{% set hideWarning = hideWarning|default(false) %}

{% block sidenavHeading %}
    <i class="ladb-icon-plus"></i> {{ 'find.new'|trans() }}
{% endblock %}

{% block form_buttons %}
    {% include 'LadbCoreBundle:Common:_form-btn-loading.part.html.twig' with { 'text':('default.save'|trans()), 'classes':'ladb-form-save-btn btn-primary btn-block' } %}
{% endblock %}

{% block body %}
    {{ parent() }}
    {% if not hideWarning %}
        {% include 'LadbCoreBundle:Common:_new-warning-modal.part.html.twig' with { 'id':'warning_find_modal', 'title':('find.new'|trans()), 'textTransKey':'find.new_warning', 'cancelPath':'core_find_list' } %}
    {% endif %}
{% endblock %}

{% block bodyJavascripts %}
    {{ parent() }}
    {% if not hideWarning %}
        <script>
            $(document).ready(function() {
                $("#warning_find_modal").modal();
            });
        </script>
    {% endif %}
{% endblock %}
