{% extends 'LadbCoreBundle:Blog/Post:_form.html.twig' %}

{% set pageTitle = 'blog.post.edit'|trans() %}
{% set formAction = path('core_blog_post_update', { 'id':post.id }) %}

{% block sidenavHeading %}
    <i class="ladb-icon-edit"></i> {{ 'blog.post.edit'|trans() }}
{% endblock %}

{% block form_buttons %}
    <a href="{{ path('core_blog_post_show', { 'id':post.sluggedId }) }}" class="btn btn-default btn-block">{{ 'default.back'|trans() }}</a>
    {% include 'LadbCoreBundle:Common:_form-btn-loading.part.html.twig' with { 'text':('default.update'|trans()), 'classes':'ladb-form-save-btn btn-primary btn-block' } %}
    <div class="checkbox">
        <label>
            <input type="checkbox" name="ladb_do_up"> {{ 'default.do_up'|trans }}
        </label>
    </div>
{% endblock %}

{% block sections %}
    {% if post.isDraft %}
        {% include 'LadbCoreBundle:Blog/Post:_draft-alert.part.html.twig' %}
    {% endif %}
    {{ parent() }}
{% endblock %}
