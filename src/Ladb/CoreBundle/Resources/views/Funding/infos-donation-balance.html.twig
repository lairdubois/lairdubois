{% extends 'LadbCoreBundle:Common:_modal-content.part.html.twig' %}

{% set id = 'infos_modal' %}
{% set title = 'funding.donation_balance'|trans()~' - '~(('month.long.'~(funding.date|date('M')|lower))|trans({}, 'date'))|capitalize~' '~funding.year %}

{% block bodyInner %}
    <p>Les <em>dons</em> représentent la somme des dons effectués sur le mois.</p>
    {% if funding.donationCount > 0 %}
        <p>Ce mois compte <strong>{{ funding.donationCount }}</strong> don{% if funding.donationCount > 1 %}s{% endif %} cumulant <strong>{{ funding.donationBalanceEur|number_format(2, ',' ,' ') }}€</strong>.
        {% if funding.donationCount > 1 %}<br>Soit un don moyen de <strong>{{ (funding.donationBalanceEur / funding.donationCount)|number_format(2, ',' ,' ') }}€</strong>{% endif %}
        </p>
    {% else %}
        <p>Ce mois ne compte <strong>aucun</strong> don.</p>
    {% endif %}
{% endblock %}

{% block footerInner %}
    <a href="#" class="btn btn-default" data-dismiss="modal">{{ 'default.close'|trans() }}</a>
{% endblock %}