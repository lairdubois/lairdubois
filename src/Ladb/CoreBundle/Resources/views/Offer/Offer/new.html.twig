{% extends 'LadbCoreBundle:Offer/Offer:_form.html.twig' %}

{% set pageTitle = 'offer.offer.new'|trans() %}
{% set formAction = path('core_offer_create') %}
{% set hideWarning = hideWarning|default(false) %}

{% block sidenavHeading %}
    <i class="ladb-icon-plus"></i> {{ 'offer.offer.new'|trans() }}
{% endblock %}

{% block form_buttons %}
    {% include 'LadbCoreBundle:Common:_form-btn-loading.part.html.twig' with { 'text':('default.save'|trans()), 'classes':'ladb-form-save-btn btn-primary btn-block' } %}
{% endblock %}

{% block body %}
    {{ parent() }}
    {% if not hideWarning %}
        {% include 'LadbCoreBundle:Common:_new-warning-modal.part.html.twig' with { 'id':'warning_offer_modal', 'title':('offer.offer.new'|trans()), 'textTransKey':'offer.offer.new_warning', 'cancelPath':'core_offer_list' } %}
    {% endif %}
{% endblock %}

{% block bodyJavascripts %}
    {{ parent() }}
    {% if not hideWarning %}
        <script>
            $(document).ready(function() {
                $("#warning_offer_modal").modal();
            });
        </script>
    {% endif %}
{% endblock %}
