{% if offer.price is defined and offer.price is not null and offer.rawPrice >= 0 %}
    <div class="ladb-offer-price">{{ offer.price }}{% if offer.priceSuffix %}{% if offer.rawPrice > 0 %} <span class="ladb-offer-price-suffix">{{ offer.priceSuffix }}</span>{% endif %}{% endif %}</div>
{% endif %}